{
  "name": "ai-resume-reviewer-api",
  "version": "1.0.0",
  "description": "REST API that scores a resume against a job description using the Gemini API",
  "main": "src/server.js",
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "test:coverage": "node --test --experimental-test-coverage"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  }
}
